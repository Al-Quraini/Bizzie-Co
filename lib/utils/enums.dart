// rankings
enum Rank { first, second, third }

// card types
enum CardTemplate {
  verticalHalf,
  horizontalHalf,
  regular,
}
// visibility
enum ActivityVisibility { anyone, connections, nearbyUsers }

// filter enum
enum SortType { newest, oldest, aToZ, zToA }

// notification type enum
enum NotificationType { request, like, newConnection, comment }

// tiers
enum Tier { newbeem, workbee, bumblebee, socialbee, queenbee }
